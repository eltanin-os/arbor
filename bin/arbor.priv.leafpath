#!/bin/execlineb -S1
importas -i ARBOR ARBOR
cd $ARBOR
foreground {
	fdclose 2
	redo repo/packages.cdb
}
backtick -ED "" path { arbor.priv.cdbquery $1 repo/packages.cdb }
ifelse { test -n $path } { echo $path }
arbor.utils.err "package ${1} not found"
