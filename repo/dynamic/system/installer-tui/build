#!/bin/execlineb -P
multisubstitute {
	importas -i DESTDIR DESTDIR
	importas -i DRTDIR DRTDIR
	elglob files "./*"
}
define TARGET "${DESTDIR}/${DRTDIR}/arcan/appl/installer"
if { install -d $TARGET }
cp -R $files $TARGET
